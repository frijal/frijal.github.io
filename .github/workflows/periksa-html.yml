name: 📋 Validasi SEO & Struktur HTML (Hanya yang Gagal)

on:
  push:
    branches:
      - main
    paths:
      - '**.html'
      - '_headers'
      - '_redirects'
      - '*.webmanifest'
  workflow_dispatch:

jobs:
  validasi:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout kode
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Buat folder mini/
        run: mkdir -p mini

      - name: Validasi HTML & SEO (laporan hanya jika gagal)
        run: |
          TANGGAL=$(date +%Y%m%d)
          LAPORAN="mini/laporan-validasi-$TANGGAL.txt"
          echo "📋 Laporan Validasi HTML & SEO (Hanya yang Gagal)" > $LAPORAN
          echo "Tanggal: $(date)" >> $LAPORAN
          echo "----------------------------------------" >> $LAPORAN

          for file in $(find . -name "*.html"); do
            GAGAL=0
            TEMP=""

            # Metadata SEO
            grep -q '<meta name="description"' "$file" || { TEMP+="❌ description MISSING\n"; GAGAL=1; }
            grep -q '<meta property="og:title"' "$file" || { TEMP+="❌ og:title MISSING\n"; GAGAL=1; }
            grep -q '<meta property="og:image"' "$file" || { TEMP+="❌ og:image MISSING\n"; GAGAL=1; }
            grep -q '<meta property="og:description"' "$file" || { TEMP+="❌ og:description MISSING\n"; GAGAL=1; }
            grep -q '<meta name="twitter:title"' "$file" || { TEMP+="❌ twitter:title MISSING\n"; GAGAL=1; }
            grep -q '<meta name="twitter:image"' "$file" || { TEMP+="❌ twitter:image MISSING\n"; GAGAL=1; }
            grep -q '<meta name="twitter:description"' "$file" || { TEMP+="❌ twitter:description MISSING\n"; GAGAL=1; }
            grep -q '<link rel="canonical"' "$file" || { TEMP+="❌ Canonical URL MISSING\n"; GAGAL=1; }

            # Struktur HTML
            grep -q '<html lang=' "$file" || { TEMP+="❌ <html lang> MISSING\n"; GAGAL=1; }
            grep -q '<meta charset=' "$file" || { TEMP+="❌ <meta charset> MISSING\n"; GAGAL=1; }
            grep -q '<title>' "$file" || { TEMP+="❌ <title> MISSING\n"; GAGAL=1; }
            grep -q '<head>' "$file" || { TEMP+="❌ <head> MISSING\n"; GAGAL=1; }
            grep -q '<body>' "$file" || { TEMP+="❌ <body> MISSING\n"; GAGAL=1; }
            grep -q '<main>' "$file" || { TEMP+="⚠️ <main> MISSING\n"; GAGAL=1; }
            grep -q '<footer>' "$file" || { TEMP+="⚠️ <footer> MISSING\n"; GAGAL=1; }

            # Heading
            H1_COUNT=$(grep -o '<h1>' "$file" | wc -l)
            if [ "$H1_COUNT" -eq 0 ]; then
              TEMP+="❌ <h1> tidak ditemukan\n"; GAGAL=1
            elif [ "$H1_COUNT" -gt 1 ]; then
              TEMP+="⚠️ Duplikat <h1> ditemukan ($H1_COUNT)\n"; GAGAL=1
            fi

            HEADINGS=$(grep -o '<h[1-6]>' "$file" | sed 's/<h\([1-6]\)>/\1/' | paste -sd " " -)
            if echo "$HEADINGS" | grep -qE '(^| )1( |$).*[^2]( |$)'; then
              TEMP+="⚠️ Urutan heading mungkin melompat: $HEADINGS\n"; GAGAL=1
            fi

            # Jika ada kegagalan, tulis ke laporan
            if [ "$GAGAL" -eq 1 ]; then
              echo "➡️ File: $file" >> $LAPORAN
              echo -e "$TEMP" >> $LAPORAN
              echo "----------------------------------------" >> $LAPORAN
            fi
          done

      - name: Linting HTML (hanya jika gagal)
        run: |
          TANGGAL=$(date +%Y%m%d)
          LAPORAN="mini/laporan-validasi-$TANGGAL.txt"
          echo "🧼 Linting HTML dengan HTMLHint..." >> $LAPORAN
          htmlhint "**/*.html" >> $LAPORAN || echo "⚠️ Ada peringatan dari HTMLHint." >> $LAPORAN
        
      - name: Commit dan push laporan ke repo      
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add mini/
          git commit -m "📋 Update laporan validasi $(date +'%Y-%m-%d')"
          git push origin HEAD
